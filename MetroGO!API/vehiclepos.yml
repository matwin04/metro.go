info:
  name: vehiclepos
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8088/api/swiftly/vehiclepos?agency=octa
  params:
    - name: agency
      value: octa
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
