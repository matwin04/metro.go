info:
  name: Add Source
  type: http
  seq: 3

http:
  method: GET
  url: https://metrogo.app/api/sources/transit
  body:
    type: json
    data: |-
      {
        "feed_id": "f-9q5-metro~losangeles~rail",
        "feed_type": "gtfs",
        "source_type":0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
