info:
  name: Add Source
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8088/api/sources
  body:
    type: json
    data: |-
      {
        "feed_id": "f-9q5-metro~losangeles~rail",
        "feed_type": "gtfs",
        "source_type":0
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
