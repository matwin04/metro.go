info:
  name: stations
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8088/api/amtraker/stations?stationId=SNA
  params:
    - name: stationId
      value: SNA
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
