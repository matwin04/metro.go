info:
  name: swiftly
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8088/api/swiftly/departures?stopId=80212
  params:
    - name: stopId
      value: "80212"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
